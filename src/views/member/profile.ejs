<%- include("../partials/head") %>
<%- include("../partials/header") %>

<main class="min-h-screen bg-slate-50 px-4 pt-28">
  <section class="mx-auto max-w-5xl py-16">
    <% const values = { ...member, ...((typeof formData !== "undefined" && formData) ? formData : (formState && formState.memberProfile) || {}) }; %>
    <%- include("../partials/flash") %>
    <div class="mb-8 flex flex-col gap-4 rounded-4xl border border-slate-200 bg-white p-8 shadow-sm md:flex-row md:items-center md:justify-between">
      <div>
        <h1 class="text-3xl font-bold text-slate-900">Member Profile</h1>
        <p class="mt-2 text-slate-600">Update your personal and professional profile details.</p>
      </div>
      <form action="/member/logout" method="post"><button type="submit" class="rounded-full border border-slate-300 px-5 py-3 text-sm font-semibold text-slate-700">Logout</button></form>
    </div>

    <div class="rounded-4xl border border-slate-200 bg-white p-8 shadow-sm">
      <form action="/member/profile" method="post" enctype="multipart/form-data" class="grid grid-cols-1 gap-4 md:grid-cols-2" data-validate-popup>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Full Name</span><input name="full_name" value="<%= values.full_name || '' %>" required class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Email</span><input value="<%= member.email %>" disabled class="rounded-xl border-slate-200 bg-slate-100 font-normal text-slate-500" /></label>
        <div class="md:col-span-2">
          <label for="member-profile-photo" class="mb-2 block text-sm font-semibold text-slate-700">Profile Photo</label>
          <div class="mb-3 flex items-center gap-4">
            <img src="<%= member.photo || '/assets/images/default-avatar.svg' %>" alt="<%= member.full_name %>" class="h-20 w-20 rounded-2xl border border-slate-200 object-cover" />
            <div class="text-sm text-slate-500">Upload a new image to replace your current profile photo.</div>
          </div>
          <input id="member-profile-photo" name="photo" type="file" accept=".jpg,.jpeg,.png,.webp,image/jpeg,image/png,image/webp" class="w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3 text-sm text-slate-700 file:mr-4 file:rounded-full file:border-0 file:bg-primary file:px-4 file:py-2 file:text-sm file:font-semibold file:text-white" />
          <p class="mt-2 text-xs text-slate-500">Optional. JPG, PNG, or WEBP up to 2 MB.</p>
        </div>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Phone</span><input name="phone" value="<%= values.phone || '' %>" inputmode="numeric" data-validation-label="mobile number" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Profession</span><input name="profession" value="<%= values.profession || '' %>" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>City</span><input name="city" value="<%= values.city || '' %>" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Date of Birth</span><input name="dob" type="date" value="<%= values.dob ? new Date(values.dob).toISOString().split('T')[0] : '' %>" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Blood Group</span><select name="blood_group" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900">
          <% bloodGroupOptions.forEach((option) => { %>
            <option value="<%= option %>" <%= (values.blood_group || '') === option ? 'selected' : '' %>><%= option || 'Select blood group' %></option>
          <% }) %>
        </select></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Gender</span><select name="gender" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900"><option value="" <%= !values.gender ? 'selected' : '' %>>Select gender</option><option value="male" <%= values.gender === 'male' ? 'selected' : '' %>>Male</option><option value="female" <%= values.gender === 'female' ? 'selected' : '' %>>Female</option><option value="other" <%= values.gender === 'other' ? 'selected' : '' %>>Other</option><option value="prefer_not_to_say" <%= values.gender === 'prefer_not_to_say' ? 'selected' : '' %>>Prefer not to say</option></select></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700"><span>Marital Status</span><select name="marital_status" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900"><option value="" <%= !values.marital_status ? 'selected' : '' %>>Select marital status</option><option value="single" <%= values.marital_status === 'single' ? 'selected' : '' %>>Single</option><option value="married" <%= values.marital_status === 'married' ? 'selected' : '' %>>Married</option><option value="divorced" <%= values.marital_status === 'divorced' ? 'selected' : '' %>>Divorced</option><option value="widowed" <%= values.marital_status === 'widowed' ? 'selected' : '' %>>Widowed</option><option value="prefer_not_to_say" <%= values.marital_status === 'prefer_not_to_say' ? 'selected' : '' %>>Prefer not to say</option></select></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700" data-marriage-field><span>Marriage Date</span><input name="marriage_date" type="date" value="<%= values.marriage_date ? new Date(values.marriage_date).toISOString().split('T')[0] : '' %>" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700" data-marriage-field><span>Spouse Name</span><input name="spouse_name" value="<%= values.spouse_name || '' %>" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700" data-children-field><span>Number of Children</span><input name="children_count" type="number" min="0" step="1" value="<%= values.children_count || values.children_count === 0 ? values.children_count : '' %>" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700 md:col-span-2"><span>New Password</span><input name="password" type="password" autocomplete="new-password" class="rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900" /><small class="font-normal text-slate-500">Leave blank to keep your current password.</small></label>
        <label class="grid gap-2 text-sm font-semibold text-slate-700 md:col-span-2"><span>Address</span><textarea name="address" class="min-h-[120px] rounded-xl border-slate-200 bg-slate-50 font-normal text-slate-900"><%= values.address || '' %></textarea></label>
        <button type="submit" class="md:col-span-2 rounded-2xl bg-primary px-6 py-4 text-base font-semibold text-white">Save Profile</button>
      </form>
    </div>
  </section>
</main>

<%- include("../partials/footer") %>
<%- include("../partials/scripts") %>
<script>
  (() => {
    const maritalStatusInput = document.querySelector('select[name="marital_status"]');
    const marriageFields = Array.from(document.querySelectorAll("[data-marriage-field]"));
    const childrenFields = Array.from(document.querySelectorAll("[data-children-field]"));

    if (!maritalStatusInput || (!marriageFields.length && !childrenFields.length)) {
      return;
    }

    function clearFields(fields) {
      fields.forEach((field) => {
        const input = field.matches("input") ? field : field.querySelector("input");
        if (input) input.value = "";
      });
    }

    function updateFamilyFields() {
      const isMarried = maritalStatusInput.value === "married";

      marriageFields.forEach((field) => field.classList.toggle("hidden", !isMarried));
      childrenFields.forEach((field) => field.classList.toggle("hidden", !isMarried));

      if (!isMarried) {
        clearFields(marriageFields);
        clearFields(childrenFields);
      }
    }

    maritalStatusInput.addEventListener("change", updateFamilyFields);
    updateFamilyFields();
  })();
</script>
